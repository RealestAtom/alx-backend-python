# Unit Testing Project

This project contains unit tests for the `utils.access_nested_map` function.

## Description
The `access_nested_map` function retrieves a value from a nested dictionary using a sequence of keys.  
For example:

```python
access_nested_map({"a": {"b": 2}}, ("a", "b"))  # returns 2

---

### ✅ Step 3: Verify
Now you can check that the file exists and is not empty:
```bash
ls -l README.md
cat README.md
